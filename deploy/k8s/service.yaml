apiVersion: v1
kind: Service
metadata:
  name: mcp-gateway
  namespace: ai-agents
spec:
  selector:
    app.kubernetes.io/name: mcp-gateway
  ports:
    - name: http
      port: 3001
      targetPort: http
